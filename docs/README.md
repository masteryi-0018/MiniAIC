# 开发前

AI编译器，用来编译一个神经网络，可以根据优化的深度分为2种：
1. 图优化。比如ort等框架会对模型进行一些算子融合，常量折叠，节点消除等
2. 算子生成。比如tvm等框架会对最终运行的kernel进行代码级的生成

本项目不涉及kernel生成，只是进行优化；输入是onnx模型，输出是flatbuf的序列化文件，被MININN使用

# 开发中

## 环境

本项目不折腾跨平台/多构建系统/多编译器了，Linux+cmake+gcc直接搞定

## 构建

```sh
cmake -B build -S .
cmake --build build
```

# 开发后